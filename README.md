ПРОЕКТ
├── server.py    ← Сервер (хранит данные, принимает запросы)
└── client.py    ← Клиент (окно с кнопками для пользователя)

Установите зависимости
pip install fastapi uvicorn pyodbc
Запуск сервера
python server.py
Запуск клиента
python client.py

# 🏪 Vending Management System

Система управления сетью торговых автоматов с веб-сервером на FastAPI и десктопным клиентом на Tkinter.

![Скриншот интерфейса](https://via.placeholder.com/800x400?text=Vending+System+Interface)

## ✨ Функционал

- Просмотр списка торговых автоматов
- Добавление новых автоматов с уникальными серийными и инвентарными номерами
- Изменение статуса (Работает / Вышел из строя / В ремонте)
- Хранение данных в MSSQL LocalDB
- Автоматическая документация API (`/docs`)

## 🛠️ Технологии

| Компонент | Технология |
|-----------|------------|
| Сервер    | FastAPI + Python |
| Клиент    | Tkinter (стандартная библиотека Python) |
| База данных | MSSQL LocalDB |
| Подключение к БД | pyodbc |


Создание базы данных (в SQL Server Management Studio)
CREATE DATABASE vending_db;
USE vending_db;
CREATE TABLE machines (
    id INT IDENTITY(1,1) PRIMARY KEY,
    serial_number NVARCHAR(100) UNIQUE NOT NULL,
    inventory_number NVARCHAR(100) UNIQUE NOT NULL,
    name NVARCHAR(200) NOT NULL,
    model NVARCHAR(100) NOT NULL,
    location NVARCHAR(500) NOT NULL,
    address NVARCHAR(500) NOT NULL,
    status NVARCHAR(50) DEFAULT 'Работает'
);
